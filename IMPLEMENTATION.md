# 薪流 · 实现记录

## 0.4.0可视化迭代

以用户截图数据量级建立可重复测试，确认问题来自未来日期补零和两日合并，以及收入/支出量级差。新图表组件替换旧Trend，保留实际日期与整数金额，支持形式/指标/粒度选择和精确数据表。新增三维度环形图；配色六选一（五种新增），总览预算卡独立按比例预警。数据库schema仍为3，已发布SQL不改。43项自动化与独立图表桌面测试通过。

## 0.3.0反馈迭代（2026-09-07）

在0.2.0基础上完成待办闭环、付款余额保护、原子自定义分类、理财收益入口、退款快捷填入、周期安全更正、统计结构、双语内置帮助及截图、目录迁移和缓存管理。39项自动化测试通过，新增桌面交互验收见test-results/iteration03-desktop.json。

数据目录迁移不更改数据库结构，schema仍为3。默认路径配置保留在Windows用户目录，新账本及默认备份移到所选父目录的SalaryFlow-data。已有自选备份目录保持配置；原账本保留。界面缓存可独立清理。D08规则改为默认保护、显式允许历史负余额，具体见PRD的0.3补充。

当前周期更正采用保守限制，不自动重归属已有交易或重划已结算历史。统计“完全无法显示”未在隔离账本复现；本轮修复了已核实的比例与日期显示问题，并通过有数据图表验收。

## 授权与开发顺序

2026-09-06，用户确认V1方案，并授权后续工程决策与开发自行推进。明确顺序：先交付中文版MVP，完成后再增加中文 / English语言选择。保留产品名称和项目目录。

## 交付顺序

0.1.0中文MVP已生成安装版与免安装版，并通过包内程序验收；随后0.2.0加入中文 / English选择。界面语言保存到设置，用户录入名称和备注保持原文，财务计算不受语言影响。使用说明见README，验证记录见VALIDATION。

## 当前工程路线

采用方案中的Electron备选：React + TypeScript + Vite界面，Electron主进程中的Node.js SQLite服务。开发机无Rust工具链，因此未安装第二套编译链。SQLite使用Electron内置的node:sqlite；金额入库为INTEGER分，业务运算和跨账户累计用BigInt，IPC金额用字符串。没有浏览器localStorage账本或模拟服务器。

主进程仅开放列举的查询、命令与文件操作；渲染层禁用Node集成、启用contextIsolation和sandbox。导入、恢复等长操作与写入共享串行队列。页面只加载打包后的本地资源。

## 与设计草案的实现差异

这些是用户授权后的实现选择，未改变转账/退款/预算等财务口径：

- 账本单例设置采用settings表。一个数据库只支持一个账本。
- 账户角色与排序存于账户/设置JSON，角色变化保留审计。首版不输出历史储蓄账户流量报表；未来如增加该指标，需追加角色有效期模型，不能直接用当前角色重算历史。
- 预算采用不可变budget_versions头和完整项目JSON快照，服务验证叶子分类及金额；保持系统、个人默认、周期三层隔离。类别仍有独立版本表和稳定ID。
- 余额视图从一笔交易的两个账户端点派生，内部转账不会保存成两条独立交易。
- 汇总优先使用SQLite整数聚合，读取为BigInt；SQLite整数中间结果溢出时回退到BigInt逐行累计，不转为浮点。
- 图表使用轻量本地SVG，不额外引入ECharts；数据量大时合并相邻日期，保留所有交易，并把负净支出画在零轴下方。
- 完整JSON导出是带格式版本和SHA-256的SQLite快照数据包，可完整还原审计和预算版本；CSV为可阅读明细，附口径清单。Excel/Markdown仍属后续P1。
- migration 1建立核心表，2增加账单延期，3加入覆盖索引。已发布脚本必须保持不变。旧库升级前通过VACUUM INTO生成一致性安全副本，校验后事务升级；失败不提交。常规备份使用Online Backup API。

真实DDL以 [core/schema.mjs](./core/schema.mjs) 和 [core/migrations.mjs](./core/migrations.mjs) 为准，后续迁移只能追加。数据模型的实现状态以本文件和源码为准；ARCHITECTURE中保留的详细表设计是原始设计基线。

## 财务功能与限制

已实现动态账户/类别、主要角色、期初与校准、收支/工资/转账/关联退款、编辑/软删除/恢复/审计、周期与模板/预算历史/结算、工资分配、每周/每月固定账单、全套时间范围、CSV映射导入/强键去重/弱重复提示/批次撤销、CSV和完整JSON导出、自动/手动备份与替换恢复。

首版人民币资产账户；信用卡负债、投资估值、目标预留、拆单、银行直连、云同步不在MVP中。备份文件未加密。金额修改会触发相关汇总重算；校准差额保留原值，历史补录后不会自动吞掉新变化。

## 验证记录

- 30组Node自动化测试通过，覆盖金额、转账守恒、退款上限、跨期修改、默认模板隔离、历史分类、校准、账单、分配累计、导入、备份恢复、迁移、损坏文件、中断恢复与翻译完整性等。
- Windows实际Electron测试覆盖首次设置、通过中文表单新增支出、六个主页面、账单弹层、浅深主题、1100像素窗口和金额守恒。截图保存在test-results。
- 100,000笔交易、20账户实测：优化前总览查询约5.0—5.3秒；覆盖索引与SQL聚合优化后约313—336毫秒（本机6次查询）。该结果是本机测试，不是所有电脑的性能保证。
- 安装包必须额外检查ASAR文件与源码一致，再运行打包后的可执行文件；不能用开发版本测试代替打包验收。

本轮尚未经过一个真实工资周期的长期使用；这属于交付后的用户试用验证，不应写成已经通过。

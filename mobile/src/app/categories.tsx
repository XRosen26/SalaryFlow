import { useState } from 'react';import { Alert,Pressable,StyleSheet,Text,TextInput,View } from 'react-native';import { useSQLiteContext } from 'expo-sqlite';import { AppScreen,Card,Divider,LoadingState,PageHeader } from '@/components/ui';import { radius,spacing,useAppTheme } from '@/constants/theme';import { useFinance } from '@/data/finance-context';import { archiveCategory,saveCategory,type Category } from '@/data/repository';
function Choice({label,active,onPress}:{label:string;active:boolean;onPress:()=>void}){const c=useAppTheme();return <Pressable onPress={onPress} style={[styles.choice,{borderColor:active?c.primary:c.border,backgroundColor:active?c.primarySoft:c.surface}]}><Text style={{color:active?c.primary:c.text,fontWeight:'800'}}>{label}</Text></Pressable>}
export default function CategoriesScreen(){const c=useAppTheme();const db=useSQLiteContext();const {snapshot,error,refresh}=useFinance();const [kind,setKind]=useState<'EXPENSE'|'INCOME'>('EXPENSE');const [name,setName]=useState('');const [group,setGroup]=useState('其他');const [editing,setEditing]=useState<Category|null>(null);if(!snapshot)return <AppScreen><LoadingState error={error}/></AppScreen>;const edit=(x:Category)=>{setEditing(x);setKind(x.kind);setName(x.name);setGroup(x.groupName)};const save=async()=>{try{await saveCategory(db,{id:editing?.id,kind,name,groupName:group});await refresh();setEditing(null);setName('');setGroup('其他')}catch(e){Alert.alert('无法保存分类',e instanceof Error?e.message:'请检查输入')}};const archive=(x:Category)=>Alert.alert('归档分类？','历史交易仍显示原分类名称；新交易和新预算不再显示该分类。',[{text:'取消',style:'cancel'},{text:'归档',style:'destructive',onPress:async()=>{await archiveCategory(db,x.id);await refresh()}}]);return <AppScreen><PageHeader title="分类管理" subtitle="分类完全动态；“其他”只是默认分类，也可以新建任意名称。"/><Card style={styles.card}><Text style={[styles.title,{color:c.text}]}>{editing?'修改分类':'新增分类'}</Text><View style={styles.wrap}><Choice label="支出分类" active={kind==='EXPENSE'} onPress={()=>!editing&&setKind('EXPENSE')}/><Choice label="收入分类" active={kind==='INCOME'} onPress={()=>!editing&&setKind('INCOME')}/></View><TextInput value={name} onChangeText={setName} placeholder="分类名称" style={[styles.input,{color:c.text,borderColor:c.border}]}/><TextInput value={group} onChangeText={setGroup} placeholder="分组名称，例如：餐饮" style={[styles.input,{color:c.text,borderColor:c.border}]}/><View style={styles.wrap}><Pressable onPress={()=>void save()} style={[styles.save,{backgroundColor:c.primary}]}><Text style={styles.saveText}>{editing?'保存修改':'新增分类'}</Text></Pressable>{editing?<Pressable onPress={()=>{setEditing(null);setName('');setGroup('其他')}} style={[styles.cancel,{borderColor:c.border}]}><Text style={{color:c.text,fontWeight:'800'}}>取消编辑</Text></Pressable>:null}</View></Card>{(['EXPENSE','INCOME'] as const).map(type=><Card key={type} style={styles.list}><Text style={[styles.title,{color:c.text}]}>{type==='EXPENSE'?'支出分类':'收入分类'}</Text>{snapshot.categories.filter(x=>x.kind===type).map((x,i)=><View key={x.id}>{i?<Divider/>:null}<View style={styles.row}><Pressable onPress={()=>edit(x)} style={{flex:1,gap:3}}><Text style={[styles.name,{color:c.text}]}>{x.name}</Text><Text style={[styles.meta,{color:c.textSecondary}]}>{x.groupName} · 点击修改</Text></Pressable><Pressable onPress={()=>archive(x)}><Text style={{color:c.expense,fontWeight:'700'}}>归档</Text></Pressable></View></View>)}</Card>)}</AppScreen>}
const styles=StyleSheet.create({card:{gap:spacing.md},title:{fontSize:17,fontWeight:'900'},wrap:{flexDirection:'row',flexWrap:'wrap',gap:spacing.sm},choice:{minHeight:42,borderWidth:1,borderRadius:radius.pill,paddingHorizontal:spacing.md,justifyContent:'center'},input:{minHeight:50,borderWidth:1,borderRadius:radius.md,paddingHorizontal:spacing.md,fontSize:15},save:{minHeight:46,paddingHorizontal:spacing.xl,borderRadius:radius.md,alignItems:'center',justifyContent:'center'},saveText:{color:'#fff',fontWeight:'900'},cancel:{minHeight:46,paddingHorizontal:spacing.xl,borderWidth:1,borderRadius:radius.md,justifyContent:'center'},list:{paddingVertical:spacing.sm,gap:spacing.xs},row:{minHeight:62,paddingHorizontal:spacing.sm,flexDirection:'row',alignItems:'center',gap:spacing.md},name:{fontSize:15,fontWeight:'800'},meta:{fontSize:12}});
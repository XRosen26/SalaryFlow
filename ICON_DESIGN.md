# 薪流图标设计规范

新版标志以一条连续的 **S 形资金流**为核心：上端代表收入进入，中段的转折代表预算分配，下端代表可沉淀的结余。连续路径也对应产品“同一笔交易只保存一次、在不同统计口径中复用”的数据原则。

## 几何规则

- 母版为 `1024 × 1024`，外框安全边距 1.8%，圆角半径 21.8%。
- 主标志由四段连续三次贝塞尔曲线组成，基准笔画宽度 96，所有连接与端点使用圆头。
- 主标志的视觉范围为 `x=230–790`、`y=180–875`；Android 自适应前景再缩放到 70%，确保进入系统遮罩安全区。
- 背景固定使用品牌森绿 `#166B52`，标志使用暖白 `#F8FCFA`，端点使用浅薄荷 `#B1E5CF`。
- 小于 32 px 时不增加文字、描边、阴影或细碎图案，保持轮廓辨识度。
- 应用内预算风险色只用于数据状态；启动器图标保持稳定，避免用户误以为安装了不同应用。

资源由 `scripts/icon.py` 生成，输出 Windows ICO/PNG、Android 自适应图层、iOS 通用图标、启动图和 favicon。

# 薪流版本说明

设计文档的 **V1** 是产品与技术方案基线，不等同于应用 v1.0。当前发布版本为 Windows **0.9.0** 与 Android **0.6.0 预览版**；iOS 共用工程已完成配置和 bundle 验证，暂无 IPA。

## Windows 0.9.0 / 移动端 0.6.0 · 2026-09-16

总览的可支出判断现在同时看计划和真实资金。金额仍取本期剩余预算与主要消费账户余额的较小非负值；卡片颜色与提示改为取“预算剩余比例”和“消费账户对剩余预算的覆盖比例”中更紧张的一项。消费账户没有资金时会直接提示余额不足，不再因为预算尚多而保持绿色。状态按安心、留意节奏、空间不多和接近上限逐级变化，并单独处理未设预算、未设主要消费账户、预算用完和超支。

PC与移动统计都加入周/月/年快捷历史选择。周期从首次记账所在周、月或年开始生成，按当前到历史倒序排列，并显示完整日期；现有工资周期、今日、近3/7/30/90天、本月、本年等入口保留。移动端补齐自定义起止日期，可使用日历或多种日期文本格式。

PC进一步提高高分屏下的说明文字、表头和状态标签字号。新增跨端回归测试覆盖资金不足颜色、分级阈值、ISO周和首次记账边界。本次不修改数据库表结构：PC schema 6、移动schema 4保持不变。

## Windows 0.8.1 / 移动端 0.5.1 · 2026-09-14

待收款现已贯通移动快速记账、PC交易记录和移动交易明细。借出与归还可单独辨认和筛选；预计归还日按临近程度分级提醒。误录记录可确认撤销，关联借出与全部归还流水会原子软删除，余额恢复且审计保留。

PC使用手册换成隔离测试账本的真实主内容区截图，并提高高分屏下的辅助文字、状态标签、表头和面板说明字号。产品内作者说明仅保留XRosen26持续迭代信息。数据库版本更新为PC schema 6、移动schema 3。

iOS安装测试不强制经过App Store：可在Mac上的Xcode使用免费Personal Team签名并安装到本人设备，但描述文件通常7天到期，需要重新构建安装。普通Windows电脑上的macOS虚拟机不作为支持方案；Apple许可仅允许在Apple品牌电脑上虚拟化macOS，且真机USB与签名流程不稳定。多人或更长期测试建议使用付费开发者账号的TestFlight或Ad Hoc。

## Windows 0.8.0 / 移动端 0.5.0 · 2026-09-14

统计增加“今日”和“最近3天”。Windows 交易记录与移动交易明细新增独立范围筛选，可组合快捷/自定义日期、最低/最高金额、类型、账户、搜索和排序，筛选在数据库查询层执行。

新增“待收款”：记录借给谁、借出账户、可选预计归还日和备注，支持分次归还及选择回款账户。借出与归还只改变账户现金和待收余额，不计入收入、支出、预算执行率或储蓄率。到期与逾期提醒保留在应用内，不主动申请系统通知权限。

移动端记账改为紧凑的分类/账户弹层，选择后自动收起；预算用完或超支时首页卡片切换为明确状态提醒。帮助中的统计界面示例已由隔离测试账本重新生成。

`mobile/` 继续作为 Android/iOS 共用工程，已加入 iOS Bundle ID、构建号、系统本地化名称和平板配置，并成功导出 iOS bundle。Windows 无法执行 Xcode 原生签名，因此本次只发布 Android APK，不提供 IPA。

## 版本沿革

| 阶段                          | 主要变化                                                                         |
| ----------------------------- | -------------------------------------------------------------------------------- |
| 设计 V1 · 2026-09-06          | 明确本地优先、整数分、单笔交易、多时间口径、动态账户、预算三层模型、审计与备份。 |
| Windows 0.1–0.3               | 中文 MVP、中英界面、待办、余额保护、退款、自定义分类、帮助与数据目录。           |
| Windows 0.4–0.6               | 统计与构成图、六套配色、预算风险色、金额隐藏、周期调整、理财估值和独立工资分配。 |
| Android 0.1–0.4               | 移动 MVP、工资分配、统计、配色、帮助、日期输入、键盘避让和图标分类。             |
| Windows 0.7 / Android 0.2–0.4 | 周期预算分析、主题图表、列表排序、分类层级、短名称与新版品牌图标。               |
| Windows 0.8 / 移动端 0.5      | 日期/金额范围、今日/近3天、待收款、紧凑记账选择器及 iOS 共用工程。               |
| Windows 0.9 / 移动端 0.6      | 安心支出双重约束、动态提醒、周/月/年历史周期、移动自定义日期与高分屏可读性。     |

更早的逐项记录见 [CHANGELOG.md](./CHANGELOG.md) 与 [mobile/CHANGELOG.md](./mobile/CHANGELOG.md)。

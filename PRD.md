# 薪流 SalaryFlow · PRD

0.9增量：总览“可支出金额”取 `max(0,min(本期剩余预算,主要消费账户余额))`。状态风险值取 `min(本期剩余预算/本期总预算,主要消费账户余额/本期剩余预算)` 并限制在0—100%；余额不足会覆盖单纯预算充足形成的绿色状态。50%及以上为安心，30%—50%提示留意节奏，15%—30%提示空间不多，低于15%提示接近上限；未设预算、未设主要消费账户、资金不足、预算用完和超支分别处理。

统计在既有口径上增加周/月/年快捷历史选择。可选项从有效交易日期和账户记账起点中的最早日期开始，按当前到历史倒序生成；本周/上周、本月/上月、今年/去年使用快捷名，其余显示明确年份和ISO周/月。PC与移动端继续支持自定义起止日期，结束日按当天完整包含。

0.6增量：预算周期口径可选择工资周期或自然月，修改规则支持立即、下周期、指定未来日期生效；统计分析继续只按交易实际日期计算。工资分配成为一级页面，所选工资用于确定到账账户和周期，上限可取本周期工资总额、源账户当前余额或自定义金额。建议先以本周期剩余预算补足主要消费账户，再把余款安排到储蓄/理财；计划不入账，用户确认已经完成银行转账后才原子创建内部转账。

账户页全部账户总资产增加独立金额开关，并继续服从全局总开关。默认账户模板增加理财账户；理财余额可按日期保存手工估值快照，同日更新覆盖当天估值，估值之后的真实转入转出继续叠加。估值涨跌不属于收入、支出或储蓄率。总览明确展示剩余预算、主要消费账户余额及两者较小的非负值“当前可安心支出”。

0.5增量：金额展示采用“全局允许显示 AND 当前卡片/账户允许显示”；总览预算、收入、总资产以及每个账户独立保存偏好。工资日可立即重算当前开放周期的结束日，或在7天后切换规则并形成安全过渡周期；均不得排除既有交易或重划含结算快照的周期。数据页提供带确认词、原生二次确认和可选删除应用备份的账本重置功能。

0.4增量：增加趋势图形式/指标/粒度选择、三维度环形图和五种额外配色。预算总览卡以本周期净支出/总预算计算预警色，不使用单个分类预算比例。图表不改变账务统计口径；饼图采用非负毛支出/收入/当前正余额，不能用负净支出占比。详见PRODUCT_DESIGN和CHANGELOG的0.4记录。

## 0.3现行规则补充（2026-09-07）

本节覆盖下文原V1中相冲突的建议。用户反馈已按优先级审查，见 [ITERATION_0.3.md](./ITERATION_0.3.md)。D08调整为：默认阻止付款账户不足的新支出/转账，修改交易排除原记录后核验交易日及当前余额；历史补录模式须在偏好设置明确开启，保留设置审计。预算超支仍允许，不等同资金不足。

理财收益收入仅包含实际收到的利息、分红及已实现收益；本金赎回或转移不得计收入。记账时可直接创建自定义收支分类。退款提供剩余可退金额快捷比例，金额仍按整数分并受原交易上限约束。

周期允许安全更正当前边界，必须容纳今天和已有交易、衔接相邻周期且没有既有结算快照；不自动重归属历史。工资日可立即作用于当前开放周期，或在7天后结束过渡周期并启用新规则；待生效规则可再次修改。

数据目录支持验证后复制并重启切换，原数据保留，已有目标账本不覆盖。清理仅渲染缓存，不清账本、审计或备份。帮助与双语说明随应用交付。

版本：V1 已确认基线｜日期：2026-09-06｜当前应用：Windows 0.9.3 / Android 0.6.3。当前实现及差异见 [IMPLEMENTATION.md](./IMPLEMENTATION.md)，使用见 [README.md](./README.md)。

总入口：[产品与技术设计方案 V1](./产品与技术设计方案 V1.md)。本文负责需求、范围、业务口径与待确认决策；交互见 [PRODUCT_DESIGN.md](./PRODUCT_DESIGN.md)，数据库与测试见 [ARCHITECTURE.md](./ARCHITECTURE.md)。

## 1. 依据与产品定位

已完整阅读《薪流 SalaryFlow产品需求.md》全部二十二节。原文作为需求输入保留，不覆盖。原文中“请开发”等文字不构成本轮编码指令；本轮以用户明确要求的设计审查为准。

产品目标：围绕工资周期，帮助个人规划支出、记录实际资金流动、检查剩余额度、积累储蓄，并用自然时间范围观察财务变化。

目标用户：主要收入相对稳定、使用多张银行卡或支付账户、愿意手工记账的 Windows 个人用户。第一版推荐单用户、单账本、人民币、离线使用。自由职业多收入可记账，但不在首版引入多套并行预算周期。该范围等待 D02 确认。

核心场景：工资到账时安排资金；日常快速记账；月底/周期末核对账单与账户余额；购买大额物品前查看剩余预算；每季度复盘净结余和支出结构。

核心价值：把“账户里有多少钱”“预算允许花多少钱”“本期实际留下多少钱”明确分开，减少重复配置，并让每个数字都能追溯到交易。

成功标准（验收目标，尚未实测）：首次配置 3—5 分钟；熟悉后普通支出记录约 15 秒内完成；任意汇总可下钻到账目；转账不改变全账本收入、支出和净结余；备份恢复后数据、余额及审计版本一致。

### 名称建议

建议保留「薪流 SalaryFlow」，副标题“预算 · 现金流 · 储蓄”。它与本版工资周期优先的定位一致，已有中文含义清楚。备选「余序」强调结余与秩序，但需要额外解释；若未来主要面向非工资收入，再评估更广义的名称。这是命名判断，未进行商标、域名或应用商店重名核验。

本轮未更名，也未重命名项目目录。D01 确认更名后，统一品牌、文档、应用标识策略，再在关闭占用的情况下迁移目录并更新项目路径。

## 2. 系统审查

严重级别：高＝可能账不平或重计；中＝体验/扩展有明显风险；低＝表达或非核心复杂度。

| 编号 | 类型/级别 | 原文问题与具体风险                             | 设计建议或待确认项                           |
| ---- | --------- | ---------------------------------------------- | -------------------------------------------- |
| R01  | 定义/高   | “储蓄”混合转入储蓄卡、收入减支出、目标进度     | 三个指标拆开，统一采用 BR06；D04             |
| R02  | 逻辑/高   | 账户类型含工资/消费/储蓄，另有角色             | 类型描述载体，角色描述用途，允许多角色多账户 |
| R03  | 计算/高   | 余额公式缺退款、校准和起始日期                 | BR02 完整公式；期初余额是特殊记录            |
| R04  | 数据/高   | 转账容易被记成两笔独立收支                     | 一笔交易保存两个账户，原子更新；收支不计转账 |
| R05  | 遗漏/高   | 退款部分退、多次退、跨期退未定义               | 关联原支出；累计上限；跨期按到账日建议 D05   |
| R06  | 数据/高   | 改历史可能只改页面，不改统计和结算             | 单一交易事实；重算关联区间；结算修订见 BR10  |
| R07  | 逻辑/中   | 100% 到 110% 没有合理超支状态                  | >100% 即超支，110% 可作更强提示；D17         |
| R08  | 定义/中   | 预算模板与工资分配例子可能被误认为同一口径     | 模板与独立计算例明确分开，均可由用户修改     |
| R09  | 定义/中   | 推荐预算总额可能被误认为固定目标               | 明确其仅为首次初始化建议，不作为收入假设     |
| R10  | 重复/中   | 默认模板、账户自定义、角色原则多节重复         | 收敛为三层预算、动态账户、分类统计三个规则域 |
| R11  | 遗漏/高   | 工资迟发/多次发/无工资、月底工资日、换发薪日   | 周期按计划规则，到账按实际日；D03/D06        |
| R12  | 数据/高   | 删账户、分类可能级联删除历史                   | 有引用时归档，FK RESTRICT；D09/D10           |
| R13  | 计算/高   | 工资分配直接用整期预算和余额，期中可能重复补足 | 用剩余预算目标和重新计算后的余额；计划不入账 |
| R14  | 遗漏/中   | 收入不足、预算零、负净支出、无收入储蓄率       | 明确缺口、无预算、负数及不可计算状态         |
| R15  | 扩展/高   | 信用卡、公积金、投资账户不是普通现金账户       | 首版现金型资产；负债、估值另设版本，D11      |
| R16  | 过度/中   | 多模板、多账户比例、推荐预算、目标同时首发     | 放 P1/P2；先完成记账—预算—核对—恢复闭环      |
| R17  | 遗漏/高   | 固定账单可能同时自动入账和手动记账             | 首版待办确认制、唯一账单实例；D12            |
| R18  | 数据/高   | 导入重复判断若只按同日同金额，会误删真实交易   | 外部唯一号优先；弱指纹只提示；人工映射确认   |
| R19  | 数据/高   | SQLite 使用中直接复制 db 文件可能丢 WAL 数据   | 在线一致性快照；恢复先校验再替换             |
| R20  | 定义/中   | “最近3个月”和“最近90天”可能被当成同义词        | 分别日历月位移与固定天数；BR08、D14          |
| R21  | 遗漏/中   | 预算删项目/暂停后旧消费可能消失                | 支出持续显示，无预算项仍纳入总实际           |
| R22  | 数据/高   | 修改分类层级会改写历史分类图表                 | 使用分类版本快照；改名/重分类区分；D10       |
| R23  | 可靠性/中 | 本地不等于加密，自动备份不等于异盘容灾         | 数据保护和备份位置独立决策 D15/D16           |
| R24  | 遗漏/中   | 第一次从月中开始、导入早于期初的数据           | 明确余额基准日，首期部分数据标签；D07        |
| R25  | 扩展/中   | 未明确报销、借贷、拆单、外币                   | P1/P2 单独建模，不能假装成普通收入抵消支出   |

## 3. 需求优先级与验收

所有 P0/P1/P2 为建议，D02 确认后成为开发范围。低优先级不等于本轮遗漏设计。

| 级别 | 功能                                                     | 验收边界                                                                             |
| ---- | -------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| P0   | 本地账本、首次设置、动态账户/类型/角色、动态分类         | 不要求三账户；可创建第四个账户；可设置多个储蓄角色和一个默认储蓄账户；已有数据可归档 |
| P0   | 收入、工资、支出、转账、关联退款、期初、校准             | 单币种、不拆单；所有金额与账户变动可回溯；退到不同的资产账户可选                     |
| P0   | 三层预算、个人默认、复制上期、空白创建、预算历史         | 改本期不改默认；改默认不改既有周期；支持金额、备注、分类替换、停用                   |
| P0   | 工资周期、工资分配建议、结算/重新打开                    | 工资记录和周期创建解耦；分配只生成本地转账记录，不执行银行操作                       |
| P0   | 总览、交易列表/搜索、账户明细、预算明细                  | 汇总与下钻一致；常用账户筛选不偷偷改变全账本总额                                     |
| P0   | 所有指定时间范围、收支分类、趋势、净结余/储蓄率          | 日期筛选完备；表格为准，基础柱/折线图；不扩展复杂预测                                |
| P0   | 简单固定账单                                             | 每月/每周规则、应用内到期待办、确认后入账；不承诺关机时提醒                          |
| P0   | 通用 CSV 导入、CSV/JSON 导出、完整快照备份恢复           | 映射/预览/重复检测；JSON 数据包可携带审计和版本；恢复不是合并                        |
| P0   | 软删除、修改历史、自动备份、migration、Windows 安装包    | 历史修订可追踪；恢复演练通过；数据目录独立于程序目录                                 |
| P0   | 明暗主题、键盘操作、中文显示、金额可读性                 | 核心路径两个主题均可用；无仅依赖颜色的状态                                           |
| P1   | 多预算模板、简单历史预算建议                             | 至少三个完整可比周期后建议，用户接受才生效                                           |
| P1   | 财务目标/虚拟资金预留、储蓄账户历史净流入                | 防重复预留，区分账户移动和目标分配                                                   |
| P1   | Excel/Markdown 导出、银行/微信/支付宝专用导入适配        | 用同一汇总接口；不同来源去重与转账匹配                                               |
| P1   | 更多账单频率、预算结转、拆分交易、分类合并工具           | 必须先扩展明细行与历史迁移规范                                                       |
| P1   | 基础信用卡账务、借还款/报销专题                          | 独立设计负债、偿还、应收状态，不用普通收支伪装                                       |
| P2   | 多账户比例分配、预测、深度分析、投资估值、公积金限制资金 | 明确现金可用性和估值变动与储蓄的区别                                                 |
| P2   | 多币种、多账本、云同步、iOS/HarmonyOS正式发行、银行直连  | Android已提供预览版；其他平台发行仍需签名、真机验证及新的隐私/同步/汇率模型          |

P0 仍有可靠性投入，不能以删除审计、备份或精确金额来压缩范围。若需进一步提速，可将固定账单放 P1；此取舍列入 D12。

## 4. 核心业务规则

以下 BR 规则构成推荐方案；涉及产品权衡的规则以 D 决策为前提。技术性不变量（例如转账不能计作收入）不因偏好变化而改变。

### BR01 金额、交易与边界

金额采用人民币分的整数；输入最多两位小数，不接受科学计数、NaN、无限值或隐式截断。普通交易金额必须 >0；方向由交易类型决定；校准用显式增减方向。单笔及任意账户绝对余额建议上限 9,000,000,000,000 分，超过拒绝并解释。累计用更宽整数并检查溢出。

一笔经济事项一个 transaction_id。首版每笔收支只有一个叶子分类，一个工资标记属于收入子类型；转账一个源账户、一个目标账户；退款指向原支出。编辑更新当前事实并追加版本，不新增一笔统计交易。草稿/未来计划不进入已发生交易；未来日期提示改为计划，P0 正式账目不允许晚于今天。

### BR02 账户余额

某账户在日期 d 日末余额 = 截至 d 的期初金额 + 收入 − 支出 + 退款 + 转入 − 转出 + 有符号校准。

期初只能有一笔有效记录，日期表示“该日开始前”的余额；同日交易在其后。账户详情显示基准日和当前计算余额。有效账户的期初不可单独删除，只能修订或与未使用账户一同停用；只有零期初且无其他交易/依赖的账户仍视为未使用。余额不作为用户可直接覆盖的字段。校准输入银行实际余额，系统计算差额，要求原因，形成调整记录；不计入收支、预算或储蓄率。校准固定记录当时差额，后续回填历史不自动改变它，提示重新核对，避免自动吞掉新数据。

导入或回填早于账户基准日的交易，必须先通过“扩展记账起点”向导：提供更早基准日的真实期初余额，替换期初版本并预览影响。不能在包含历史累计金额的期初余额之上重复叠加旧交易。

隐藏账户只影响列表展示；归档账户禁止新普通交易但历史仍统计，可通过显式恢复或历史修订处理。有历史记录、账单、默认角色或余额的账户禁止直接物理删除。未使用且零余额账户可软删除。归档前处理默认角色和未来账单，不强迫用户把真实余额伪造为零。

### BR03 收入、支出、转账与退款的影响

| 类型         | 账户影响       | 收入 I | 净支出 E | 预算实际 | 净结余 S |
| ------------ | -------------- | -----: | -------: | -------: | -------: |
| 收入/工资 x  | 收款账户 +x    |     +x |        0 |        0 |       +x |
| 支出 x       | 付款账户 −x    |      0 |       +x |       +x |       −x |
| 内部转账 x   | 源 −x，目标 +x |      0 |        0 |        0 |        0 |
| 关联退款 r   | 收款账户 +r    |      0 |       −r |       −r |       +r |
| 期初/校准 a  | 对应账户 +a    |      0 |        0 |        0 |        0 |
| 虚拟目标预留 | 无资金变化     |      0 |        0 |        0 |        0 |

内部转账限定本账本管理的两个不同账户。同账户转账拒绝。给他人的付款通常是支出；借给别人需要未来应收模型。转账手续费是独立支出，可与转账属于一个操作批次，不能藏进源/目标不相等的转账金额。

现金型账户余额不足时默认阻止支出或转账并显示缺口；只有用户为补录旧账明确开启历史补录模式后才允许负余额并持续提示（D08）。工资分配助手也不会生成超过当前可分配余额的计划。

退款必须关联有效支出，日期不早于原支出，累计有效退款 ≤ 原金额，可分多次、可退往另一资产账户；退款沿用原支出分类版本。商家额外补偿超出本金，超出部分作为单独收入，不扩大退款上限。

有退款的原支出改小/删除时，先展示依赖项：编辑金额需仍覆盖退款总额；删除需同时撤销关联退款或先处理依赖，整个操作原子完成；禁止产生孤儿退款。修改原分类需联动所有关联退款，展示跨期影响，保留版本历史。未找到原交易的退款导入保留在待处理区，允许先补录原支出；不直接伪装成收入。

### BR04 三层预算与分类

系统种子模板仅在初始化时复制成用户个人默认模板，携带 seed_version。系统升级不得覆盖个人默认或周期预算。用户主动“恢复推荐值”必须预览且只新建个人模板版本。

个人默认模板保存为版本；一个账本一个当前默认版本。创建周期时复制具体版本为当期预算快照。修改当期默认选择“仅本周期”；选“同时更新个人默认”会在同一数据库事务中分别创建周期预算版本和个人模板版本，已有其他周期不受影响。保存全部当前预算为个人默认，还是仅同步本次改动，建议采用“同步当前完整预算”，提交前展示差异，见 D18。

复制上一周期默认复制最后有效预算，不复制实际交易、结算状态、结转或原始预算版本。空白预算合法；未来多模板沿用相同版本机制。每次周期预算修改保留完整快照、时间、原因、操作者，第一次版本不可覆写。界面并排提供“初始预算”“当前预算”。

预算仅对支出叶子分类分配，父类只能汇总，防止父子双重计额。分类与账户互不绑定。删除/暂停预算项目只让该项目当前额度为 0，不隐藏其历史实际；无预算的分类消费并入“未设预算”。调整预算分类是将旧行停用、新行加入，不自动重新分类历史交易。

类别重命名/换父类产生新分类版本，历史交易与预算使用原版本；新交易采用新版本。P0 不提供静默批量重分类。修改一笔历史交易的分类属于显式修订。历史分组使用当时的父级版本和标签；同一稳定分类 ID 的预算归属不因改名失配。收入、支出分类语义不能互改，有引用时归档后新建。

### BR05 预算计算与状态

对周期 c：总预算 B = 当前预算版本所有启用行额度之和；实际净支出 E_c = 归属 c 的有效支出 − 归属 c 的有效退款，包含未设预算支出；剩余预算 R = B − E_c，不取 max(0,R)，超支显示负数。

分类 g 使用相同公式。执行率 = E_g/B_g ×100%，仅 B_g>0 时有值；B_g=0 时显示“未设预算”，有支出提示“未设预算支出”；没有 NaN、Infinity 或虚假的 0%。退款导致 E_g<0 时保留负值，说明“本期退款超过支出”，不截断财务原值。

状态建议：E<0 退款净额；0≤执行率<80% 正常；80%≤执行率<100% 接近预算；=100% 已用完；>100% 超出预算。>110% 可加强文字，但不改变财务定义。金额判断用整数比较，显示百分比四舍五入到一位小数，颜色不能成为唯一提示。

首页“还能花”展示预算剩余，同时并列“可支配账户余额”；前者是规划额度，后者是流动资金，不能混称。预期收入未确认时预计结余显示“待设置预期收入”，不把账上已有资产当收入。

如果用户设置本周期预期收入I_plan，计划净结余=I_plan−B；进行中的预计期末净结余=预计全期收入−(已发生净支出+预计后续净支出)，所有预测输入明确标记且不计实际报表。P0只要求前一个简单计划值，后续支出预测P2再做。

### BR06 储蓄、资产与储蓄率

推荐主指标称“本期净结余（储蓄）”：S = I − E；储蓄率 = S/I，仅 I>0 时计算。I=0 时显示“—（本期无收入）”，即使退款令 S>0 也不除零。S<0 显示负储蓄率；退款可能令比率超过100%，保留原值并解释。本定义是个人现金收支结余，不是投资收益或财富增长率，D04/D05 确认。

“转入储蓄账户”是资金位置变化，不能加到 S。“目标已预留”是对现有钱的用途分配，也不能增加 S。P0 主指标采用 I−E，P1 可增加独立的储蓄账户净流入报表，必须以交易时点的角色记录而非当前角色重写历史。

P0 资产合计 = 全部纳入账本的资产账户余额之和，包含隐藏/归档账户；常用账户面板另标“常用账户小计”。P1 支持负债后区分总资产、总负债、净资产，信用卡偿还作为资产与负债之间的内部变动，不能再次计支出。

P0 校验关系：期末资产 − 期初资产 = I−E + 范围内期初变动 + 校准净额（采用同一账户集合和时间端点）。其中范围起点以前已有期初余额已经包含在期初资产中，不再加一次。

### BR07 工资周期与 transaction_date / budget_cycle

transaction_date 是实际发生的本地日历日期，created_at 是录入时间，二者不可混用。工资晚到只影响其实际到账日所在周期，不自动移动周期边界。每笔交易只保存一次；各报表查询同一事实。

推荐一个账本一条不重叠的工资周期时间线，区间内部为 [start_date,end_date_exclusive)。例：9月10日—10月9日存为 [2026-09-10,2026-10-10)。工资日为31号时月末截短，例如 [2026-01-31,2026-02-28)，下一周期 [2026-02-28,2026-03-31)，无重叠和空洞。预算周期规则可立即应用当前开放周期、从下周期启用或指定未来日期启用，并预览必要的过渡短周期；支持工资周期与自然月口径，均保留既有交易且不重排已结算历史，D03。

P0 收入、支出、退款的 budget_cycle_id 根据 transaction_date 匹配唯一周期；不允许人工任意挂到另一周期。转账、期初、校准不消耗预算，budget_cycle_id 为 NULL；分析工资周期资金流时仍用日期筛选。缺少周期时先生成覆盖该日期的草稿周期，用户确认预算来源；更早数据使用空白预算并标记历史补录，不能把今天默认值假装成当时计划。

退款建议归到退款到账日的周期，原交易关联用于退款上限与分类，不用于强制回写原周期。这使按日期的现金流与周期预算可以直接核对；另一方案是冲减原消费周期，但会造成“历史预算被未来退款改变”，列 D05 等待选择。可在 P1 提供原消费成本视图，需单独命名且不改变事实交易。

修改 transaction_date 必须重新匹配 budget_cycle_id，并更新新旧周期、自然日期窗口和两侧账户余额；涉及结算周期先重新打开。仅按不同统计口径读取数据，不为周/月/季度复制交易，不建立长期维护的月度交易副本。

### BR08 所有时间范围

账本日历时区建议固定 Asia/Shanghai，日历日字段不随电脑时区自动漂移；系统时间戳 UTC。自然周周一开始。UI 选截止日含当天，内部统一转换为次日的排他终点。以下默认锚点 T 为今天，可在分析页指定参照日期。

| 口径           | 内部范围                                             |
| -------------- | ---------------------------------------------------- |
| 工资周期       | 选定周期 [start,end_exclusive)                       |
| 自然周         | 所选周周一至下周一                                   |
| 自然月         | 所选月1日至下月1日                                   |
| 自然季度       | 1/4/7/10月1日至下一季度首日                          |
| 自然年         | 所选年1月1日至次年1月1日                             |
| 最近7/30/90天  | [T−(N−1)天,T+1天)，含今天 N 天                       |
| 最近3/6/12个月 | [shift_months(T+1天,−N),T+1天)，月移位遇无同日取月末 |
| 自定义         | [用户开始日,用户结束日+1天)                          |

例：T=2026-09-06，最近90天为6月9日—9月6日；最近3个月为6月7日—9月6日。这里“最近几个月”是滚动日历月，若用户希望最近几个完整自然月，另列选择 D14，不能偷换定义。当前未结束自然期间只统计已发生账目，显示“截至今天”；无数据与零净额分别提示。

范围统计的 I/E/S 按 transaction_date 计算；只有预算页面与明确工资周期分析展示预算执行率。自然月不把与其相交的两个工资周期预算简单相加，也不按天擅自摊分预算。账户筛选模式分别显示外部收入/支出与内部转入/转出，储蓄率默认全账本；选单账户时隐藏储蓄率，避免把跨账户移动误读成个人结余。

### BR09 工资分配助手

选择已记录工资、主要消费目标账户、主要储蓄账户。初始预算资金目标采用剩余预算，默认全账本分类消费，不与付款账户绑定。所有值可解释，用户决定哪些余额真正可用于消费。

单目标建议式：目标资金 T=max(0,B−E_c)；消费可用余额 A=max(0,该消费账户当前余额−明确预留额)；补足需求 N=max(0,T−A)。P0 无目标预留功能时预留额为0；用户可手工调整目标金额。工资源当前可分配 F=min(用户选择本次工资分配上限,max(0,源账户余额−用户保留额))。

源账户不等于消费账户：补足=min(N,F)；短缺=max(0,N−F)；建议转储蓄=max(0,F−补足)。源账户等于消费账户：不生成自转账，须在源余额中保留 T，建议转储蓄=min(F,max(0,源余额−保留额−T))，不足目标显示缺口。源等于储蓄账户则剩余只标记“留在原账户”，不记转账；消费与储蓄默认指向同账户时提示无法区分用途，让用户修改或跳过分配，不伪造两笔转账。

原例的独立情境：预算5200、尚无支出、消费余额700、工资10000且源余额10000 → 补足4500、转储蓄5500。若已支出1200，目标4000，余额700 → 补足3300。系统模板与工资分配情境相互独立，分配时始终读取当期预算、实际余额和本次选择的工资上限。

助手只提供建议，不访问银行执行付款。用户实际完成后逐项确认，生成普通内部转账；已手动记录可关联现有交易。各项有幂等标识，重复点击不重记。每笔工资最多一份有效未完成计划；重新计算仅替换未完成项，已确认转账保留。计算F时，用户选择本次工资分配上限还需扣除该工资下累计已确认的分配金额，不能另建计划重复获得整笔工资额度。删除或修改关联工资、转账时需同步校验所有计划的剩余额度和状态。计划与实际分开，刷新余额/预算变化后重新预览；已确认项不能重复执行。允许部分完成与取消剩余。高级比例规则推迟 P2。

### BR10 周期创建、结算、修改与删除

周期状态：草稿 → 开放 → 已结算；已结算可通过显式“重新打开并说明原因”回到开放。进入新周期只自动生成草稿和预算建议，确认后开放；可以同时有多个历史开放周期，“当前”按日期决定，而不是数据库只允许一个开放状态。

结算不生成收入、支出或转账；先提示未处理账单、未分类、账户差异及未完成分配，允许记录解释后结算。保存结算时各指标、预算版本、账本修订号和规则版本，作为“当时结算快照”。后续历史修改保留旧快照并标“已被修订”，重新结算产生新版本。实时分析始终用当前有效交易，导出注明是否为历史快照。

跨周期退款若采用到账日法，不会仅因关联已结算原交易而重新打开原周期；若修改原金额/分类导致退款依赖项变化，则必须重新打开受影响周期。日期或周期规则修正需同时校验边界、归属与结算状态，P0 不提供大范围重排旧周期工具。

历史影响需分两层：交易发生日/预算归属直接影响的已结算周期必须重新打开；交易引起的账户余额变化会传播到更晚的日期，即使是无budget_cycle_id的转账、期初或校准也一样检查。更晚周期的历史余额快照标“受上游历史修订影响”，保留当时值并提供当前重算值，不强制批量重开所有后续周期；如用户需要替换结算快照，再逐周期重结算。这是D23推荐策略，不能仅刷新被编辑交易所在月。

交易软删除排除当前余额/统计，保留历史并允许恢复。恢复也是一次受校验的变更：重新验证余额基准日、原交易、退款上限、周期状态和唯一性；不直接撤销删除标记绕过业务规则。一次操作涉及多个交易时以操作批次原子写入。

结转预算是“下一期计划额度调整”，不会创造资金；P0 默认不自动结转，D13 待确认。未花完的现金仍留在真实账户中。

### BR11 固定账单

规则与实际交易分离。规则产生唯一到期实例（rule_id,due_date）；应用启动补齐遗漏实例，只生成待办。确认金额/账户/实际发生日期后产生一笔支出；允许关联已记录交易、跳过、延期或修改当次金额，均保留处理痕迹。修改规则默认只作用于尚未生成的未来实例；对已生成未支付实例另外预览选择更新，已支付不改写。每月29/30/31日遇短月建议取月底。

删除账单对应支出时，将实例退回待确认状态并提示；不能让账单显示已支付而交易消失。P0 不自动后台扣款，不要求 Windows 常驻服务。跨期支付按实际日归属，账单到期日不能替代 transaction_date。

### BR12 财务目标（P1 设计）

目标包含名称、目标金额、可选截止日、优先级、状态。推荐“虚拟预留”模式：从某账户可用余额划出用途额度，不生成收入/支出/转账；同一分不能同时预留给两个目标。目标进度=当前有效预留额/目标金额，另可显示已用于目标的消费，二者不可重复相加为现有资金。

增减预留、从目标付款、目标间重新分配形成预留事件；目标消费需要释放对应预留并关联一次真实支出。账户余额减少到低于预留合计时显示缺口并要求调整，不能伪装仍已足额储备。目标完成/取消释放预留不改账户余额。D19 确认后再细化开发。

### BR13 导入、导出与备份

导入：选择文件 → 明确账户基准日和字段映射 → 标准化金额/日期/方向 → 暂存预览 → 检查唯一号、相似交易、退款和转账依赖 → 确认 → 一个批次原子提交 → 结果报告。P0 通用 CSV 使用一行一交易、转账含两账户，不从两行支出/收入自动猜转账。坏行必须修正或显式排除，不能悄悄丢弃。

外部来源+来源账户键+外部交易号为强键；文件哈希标记重复文件。没有唯一号时，日期/金额/方向/账户/标准化商户备注指纹仅标“疑似重复”，用户可保留真实同额交易。删除过的外部交易再次导入仍提示，允许明确恢复，不能绕过去重。导入撤销先检查后续修改、退款、账单和结算依赖，依赖存在时阻止一键撤销并列出原因。

导出：CSV 交易明细，JSON 可移植完整数据包（明确版本），P1 XLSX 多表汇总和 Markdown 周期报告。CSV/XLSX 文本字段预防公式注入；金额保持两位小数；JSON 金额用“分”的十进制字符串；任何报表注明时区、起止日、口径、退款处理、生成时间和修订号。

备份：完整一致性 SQLite 快照+版本/校验清单，包含已删除记录、审计、预算历史和本地配置；不把报表当备份。恢复默认整体替换而非合并，先另存当前安全快照，再验证备份、迁移临时副本并替换，失败回到原库。导入是增量业务操作，与恢复分开。自动备份仅在应用运行时触发，启动补做，升级/恢复/大批量导入前额外备份。建议日备份保留30份、月备份12份、迁移前至少最近3份，D15 待确认；删除旧备份必须在新备份验证成功以后。

## 5. 初始推荐预算（仅配置种子）

| 组         | 明细（元）                                          | 小计（元） |
| ---------- | --------------------------------------------------- | ---------: |
| 固定生活   | 房租2400、水80、电180、燃气70、通信160、软件订阅260 |       3150 |
| 餐饮       | 买菜700、食堂/外食/外卖1200、零食饮料280            |       2180 |
| 日常生活   | 交通360、日用品240、理发120、衣物480                |       1200 |
| 社交娱乐   | 聚餐520、礼物360、娱乐420、旅游650                  |       1950 |
| 成长投资   | 学习650                                             |        650 |
| 医疗与备用 | 医疗260、备用480                                    |        740 |
| 总计       | 20个叶子项目；所有项目可编辑/停用                   |       9870 |

“成长投资”是消费分类的显示名称，不代表投资资产或买入证券。预期工资未提供，不推断预算是否可负担。首次引导必须显示总额而不是只展示零散建议值。

## 6. 产品决策记录

用户已确认方案并授权后续工程决策。以下保留评审时的选择及取舍；采用推荐项，例外和落实如下：D20采用Electron备选和Windows x64；D16本版不加密；D21提供含运行时的离线安装版与便携版；D22首版CSV及完整JSON。用户个人收入、账户和工资日仍由首次设置录入。先完成中文0.1.0，再在0.2.0增加中文 / English选择。

| ID  | 需要确认的选择                     | 推荐及理由                                             | 另一选择/代价                                        |
| --- | ---------------------------------- | ------------------------------------------------------ | ---------------------------------------------------- |
| D01 | 保留薪流还是更名                   | 保留薪流 SalaryFlow，当前目录不动                      | 余序或用户指定名；更名需统一路径和品牌               |
| D02 | 首版范围是否接受本表               | 单人单账本人民币，可靠性优先的 P0                      | 把信用卡/目标/专用导入前移会增加设计和测试量         |
| D03 | 工资周期固定日期还是跟随实际到账   | 固定计划日、短月取月末；可立即、下周期或指定日期生效   | 到账触发会导致长度不定、迟发回溯复杂                 |
| D04 | “储蓄”主指标                       | 收入−净支出，显示“净结余（储蓄）”                      | 以转入储蓄账户衡量需另名，不能代替全账本储蓄率       |
| D05 | 跨周期退款冲减哪期预算             | 按退款到账日冲减当期，现金流一致                       | 冲减原周期更符合原购买成本，但历史结算会变           |
| D06 | 迟发工资、奖金是否手工指定预算周期 | 按实际日，工资作为收入子类型                           | 手工指定要引入归属模式、差异对账与更多规则           |
| D07 | 中途开始与首期预算                 | 正常日期周期，完整模板可手改，标数据不完整             | 自动按天折算会误摊房租等固定费用                     |
| D08 | 普通现金账户出现负余额             | 默认阻止；仅在明确开启历史补录模式后允许并提示         | 始终允许容易掩盖漏记收入或错误期初                   |
| D09 | 有历史账户的“删除”                 | 归档保留历史；只允许未使用账户软删除                   | 连历史清除会破坏可追溯性，须另做危险清理工具         |
| D10 | 分类改名/层级对历史是否生效        | 历史快照保留，新交易用新名称                           | 全历史按新分类看更统一，但需重分类预览与结算修订     |
| D11 | 信用卡/投资/公积金首版需要哪些     | 已增加理财估值快照；信用卡与公积金模型继续后置         | 信用卡需负债/偿还；公积金需受限资金模型              |
| D12 | 固定账单 P0 与确认方式             | P0 简单每周/每月待办，确认入账                         | 推迟 P1 更快；自动入账更省事但更易重复               |
| D13 | 未用预算结转                       | P0 不自动结转                                          | 自动结转需区分金额、来源、上限和对模板的影响         |
| D14 | 最近几个月/自然周/日历时区         | 滚动日历月、周一、Asia/Shanghai                        | 完整自然月/周日/其他时区会改变边界，需明确标签       |
| D15 | 自动备份位置和保留                 | 应用内日/月快照+用户选择异盘目录；30日/12月            | 同盘简单但不防硬盘故障；异盘可能离线、需失败提示     |
| D16 | 数据库与备份是否必须加密           | 本地权限隔离作为基线；需用户确定威胁场景               | 密码加密需密钥/恢复设计；应用锁不等于文件加密        |
| D17 | 超预算阈值                         | >100% 即超支，80%提醒                                  | 保留110%只能作提醒强度，不能推迟超支定义             |
| D18 | “同时更新默认预算”同步范围         | 预览后保存本期完整预算为新默认                         | 仅同步改动更细，但已有模板差异更难解释               |
| D19 | 目标模型与优先级                   | P1 虚拟预留，不要求每目标一张卡                        | 只追踪目标账户余额更简单，但多个目标共用资金易重计   |
| D20 | 桌面技术与 Windows 版本            | 已采用 Electron + React + TypeScript，发布 Windows x64 | Tauri未采用；Windows ARM64仍需独立构建与验证         |
| D21 | 离线安装与发布范围                 | 本人试用先做 NSIS 安装包；确认是否需完全离线安装       | 内嵌 WebView2 离线安装器增加包体；公开分发需签名安排 |
| D22 | 第一批导入和导出格式               | P0 通用 CSV/完整JSON，P1 Excel/Markdown与专用账单      | 若用户已有大量微信/支付宝数据，应前移对应适配器      |
| D23 | 结算后的历史修改                   | 重新打开+原因+旧快照保留                               | 完全锁定须通过更正交易处理，学习成本更高             |
| D24 | 工资分配资金池                     | 默认本次工资上限、消费账户余额补足、保留额可调         | 合并多个消费账户/动用历史存款需更复杂可用资金设置    |

## 7. 变更与维护约定

需求以 BR/功能优先级及 D 决策编号引用。确认后在本节记录决策日期和选择，更新关联交互、模型、测试与 CHANGELOG。同一计算规则只在此处定义，其他文档引用并给实现方式；未决项不得在正式开发中静默变成默认结论。

决策记录：2026-09-06，用户确认V1并授权自主实施；D01—D24按本节记录落实，P1/P2维持后续范围。原文其他“待确认”字样属于评审历史，现行取舍以本节及IMPLEMENTATION为准。

## 存钱计划与账户统计（0.9.3）

- 存钱计划支持目标金额、当前进度、剩余金额、可选目标日期和备注。
- 自动模式可关联一个或多个账户，当前进度取关联账户的实际余额之和；手动模式由用户维护当前进度，两种模式互斥。
- 账户间转账不计收支，也不会让同一组关联账户的计划进度重复增加。
- 统计可限定账户：收入按入账账户、支出按付款账户、退款按退回账户聚合。
- 退款在数据层继续独立保存并关联原支出；交易列表在原支出上显示累计退款状态。
